import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Dashboard = () => (
    <Card>
        <CardHeader title="Welcome to the Admin Page" />
        <CardContent>Dlvery Admin</CardContent>
    </Card>
);

export default Dashboard;